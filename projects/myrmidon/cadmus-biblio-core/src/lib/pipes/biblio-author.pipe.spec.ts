import { BiblioAuthorPipe } from './biblio-author.pipe';

describe('BiblioAuthorPipe', () => {
  it('create an instance', () => {
    const pipe = new BiblioAuthorPipe();
    expect(pipe).toBeTruthy();
  });
});
